using System;
using Xunit;
using BookwormsAPI.Services;
using BookwormsAPI.Contracts;
using Moq;

namespace BookwormsAPI.Tests
{
    public class RequestServiceTest
    {
        // [Fact]
        // public void GetDateDueTest()
        // {
        //     // Arrange
        //     DateTime dateToday = DateTime.Now;
        //     var expectedDate = dateToday.AddDays(28);
        //     var actualDate = new DateTime();
            
        //     // Act
        //     var service = new Mock<RequestService>();
        //     service.Setup(x => x.GetDateDue(dateToday)).Returns(actualDate);
            

        //     // Assert.Equal(expected, actual)
        //     Assert.Equal(expectedDate, actualDate);
        //     //service.Verify(x => x.GetDateDue())
        // }
    }
}